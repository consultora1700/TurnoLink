# ===========================================
# TURNERO SAAS - Environment Variables
# ===========================================

# General
NODE_ENV=development
APP_URL=http://localhost:3000
API_URL=http://localhost:3001

# Database (PostgreSQL)
DATABASE_URL=postgresql://turnero:turnero123@localhost:5432/turnero_db?schema=public

# Redis (Cache & Sessions)
REDIS_URL=redis://localhost:6379

# JWT Authentication
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d
JWT_REFRESH_SECRET=your-refresh-secret-key-change-in-production
JWT_REFRESH_EXPIRES_IN=30d

# NextAuth
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-nextauth-secret-change-in-production

# AWS S3 / Cloudflare R2 (Image Storage)
S3_ENDPOINT=
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
S3_BUCKET_NAME=turnero-uploads
S3_REGION=auto

# Email (Resend)
RESEND_API_KEY=
EMAIL_FROM=noreply@turnero.app

# WhatsApp Business API (Twilio)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_NUMBER=

# Super Admin (First Admin User)
SUPER_ADMIN_EMAIL=admin@turnero.app
SUPER_ADMIN_PASSWORD=admin123456

# Rate Limiting
RATE_LIMIT_TTL=60
RATE_LIMIT_MAX=100

# 2FA (TOTP) Encryption
# Generate with: openssl rand -hex 32
TOTP_ENCRYPTION_KEY=your-64-char-hex-key-for-aes256-encryption-change-in-production

# Mercado Pago OAuth (Sandbox)
# Get credentials from: https://www.mercadopago.com.ar/developers/panel/app
MP_CLIENT_ID=your_sandbox_client_id
MP_CLIENT_SECRET=your_sandbox_client_secret
MP_REDIRECT_URI=http://localhost:3000/api/mercadopago/callback

# Mercado Pago OAuth (Production) - Optional
# Uncomment and fill when ready for production
# MP_PROD_CLIENT_ID=your_production_client_id
# MP_PROD_CLIENT_SECRET=your_production_client_secret

# Mercado Pago Credentials Encryption
# Generate with: openssl rand -hex 32
MP_ENCRYPTION_KEY=your-64-char-hex-key-for-mp-credentials-encryption
